package org.projectusus.core.filerelations.model;

public class PackageRelation extends Relation<Packagename> {

    public PackageRelation( Packagename source, Packagename target ) {
        super( source, target );
    }
}
